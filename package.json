{
  "name": "whatsapp-bulk-messenger-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install": "npm install --workspaces",
    "build": "npm run build --workspace=frontend",
    "start:backend": "npm start --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend"
  },
  "devDependencies": {}
}
